import { StyleSheet } from "react-native"

export const GStyles = StyleSheet.create({
	appContainer: {
		flex: 1,
		backgroundColor: "#fff",
	},
	container: {
		flex: 1,
	},
	header: {
		fontSize: 20,
		color: "gray"
	}
})